/*
import { AppDataSource } from "../config/configDb.js";
import Historial from "../entity/historial.entity.js";

export async function registrarHistorial({ usuario, accion, tipo, referenciaId }) {
    const repoHistorial = AppDataSource.getRepository(Historial);
    await repoHistorial.save({
        usuario,
        accion,
        tipo,
        referenciaId
    });
}
*/
//* no se si usarlo